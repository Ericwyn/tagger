# 前端操作与真实能力对齐

## 已完成

- 曲目技术信息中的复制按钮已接通浏览器剪贴板，并在成功或浏览器拒绝时给出提示。
- 审核页移除没有实现行为的“修改策略”按钮；字段逐项勾选和候选抽屉的全选/全不选才是当前真实策略入口。
- 设置页移除没有后端实现的“添加自定义来源”按钮，改为明确说明当前可配置的是内置策略。
- 曲库扫描模式、定时对账、符号链接和忽略规则改为只读能力说明，避免用户修改不会生效的假设置。

## 验证

- 新增曲目详情复制相对路径测试。
- 前端类型检查、99 个测试和生产构建通过。
