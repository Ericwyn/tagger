# 审核连续操作与旧曲目兼容

## 本次完成

- 审核页点击“接受候选”或“跳过此曲”后，立即将焦点移动到下一首可操作曲目；保留“更换候选”在当前曲目内循环，方便先比较候选再确认。
- 审核决策、选中的候选、字段 checkbox 和封面选择写入匹配任务；重新打开任务时恢复完整审核快照。
- 新增 SQLite `00009_match_review_snapshot` 迁移，保存字段选择和封面选择，不依赖浏览器本地状态。
- 兼容迁移前的扫描数据：真实 API 客户端将缺失或 `null` 的数组字段（包括 `composers`、`lyricists`、MusicBrainz artist ID）归一化为空数组，字符串归一化为空字符串；标签、歌词、封面和历史恢复响应统一处理。

## 验证

- 前端 `npm run lint`、50 个 Vitest 用例通过。
- Go `go test ./...`、`go test -race ./...` 通过。
- 使用 `/home/ericwyn/Downloads/TestMusic` 的 MP3、FLAC、WAV 集成测试通过。
- `make build` 成功生成包含前端 dist 的 `dist/tagger` 单二进制。

## 边界

旧数据库不会因为缺少新增字段而要求用户立即重扫；重扫仍可用于刷新文件当前的全部元数据。归一化只负责保证旧记录安全展示和编辑，不会凭空生成标签内容。
