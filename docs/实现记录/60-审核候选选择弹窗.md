# 审核候选选择弹窗

## 已完成

- 审核详情的“更换候选”不再把候选列表插入对比表上方，避免用户为了选择来源而滚动离开底部操作区。
- 点击后使用应用内模态层展示当前曲目的全部候选，候选项保留封面、来源、外部 ID、匹配度、匹配等级以及封面/歌词能力信息。
- 选择候选后立即关闭模态层、回到对比表，并按新候选重新计算默认差异字段；点击遮罩或按 Escape 可取消选择。

## 验证

- ReviewPage 测试覆盖弹窗打开、Escape 关闭、重新打开和直接选择指定 Apple Music 候选。
