# README 开源项目文档

## 背景

原 README 主要是开发中的功能清单，缺少项目定位、核心价值、典型工作流、数据安全边界和新用户启动路径，不适合作为开源项目主页。

## 本次更新

- 用“本地优先、先审核再写入、安全可审计、策略隔离”重新组织项目叙事。
- 增加 MP3/FLAC/WAV、标签/歌词/封面、批量任务、多曲库和单二进制部署的能力矩阵。
- 增加 Mermaid 工作流和后端/前端/SQLite/Provider 的架构图。
- 增加数据源策略表、快速开始、CLI/环境变量配置表、开发命令和项目目录说明。
- 明确写入安全模型、备份提醒、实验性非官方数据源边界，以及当前不做的功能。
- 增加灵感项目、贡献方式和许可证现状说明，避免把未声明的许可证写成既定事实。

## 验证

- `git diff --check`
- 对照 `internal/config`、`Makefile`、`docs/任务列表.md` 和当前 Provider/任务实现核对命令与能力描述。
